# deps / build
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
next-env.d.ts

# env — never commit secrets (public repo, private data)
.env
.env.*
!.env.example

# data — never commit personal data or DB dumps
exports/
*.dump.json
# DB dumps only — drizzle/*.sql migrations MUST be committed (Vercel needs them at build time)
*.dump.sql
exports/*.sql
*.dump

# tooling
.vercel/
.DS_Store
coverage/
qa/shots/

# extra
*.pem
/build
.pglite/
qa/*.html
qa/loaders-shots/
qa/focus-shots/
qa/full-shots/
